 <!-- Print the user membership status -->
    <h2>Your membership status is:

            <% if (user.admin) { %>
            <span class="membership">FACULTY</span>
            <h4>You may revert your status</h4><form action="/demote" method="post"><button>here</button></form>
            <% } else { %>
                   <span class="membership">STUDENT</span>
                   <h3>If you are faculty, type in the password provided by the Dean</h3>
                   <h4>HINT: Who is the best Dean in the whole world? (all lowercase, no spaces)</h4>
                   <form method="post">
                    <input type="password" name="profilePassword">
                    <a><button>Submit</button></a>
                </form>
            <% } %>
    </h2>

